using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[RequireComponent(typeof(Collider2D))]
public class Collidable : MonoBehaviour
{
    /*
     * GameObjects having this component will collide with any gameObject that has the PlayerMovement component attached to them
     * 
     * */
}
